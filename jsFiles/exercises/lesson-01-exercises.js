"use strict";
/*
 * LESSON 1 EXERCISES
 *
 * Complete the tasks below. Run `tsc` after each change.
 * Do not look at the solution until you have attempted every task.
 */
// 1. Create a constant named `firstName` containing your first name.
const firstName = "Replace this text";
// 2. Create a variable named `score` with the value 0.
//    Increase it to 10 by assigning a new value.
let score = 0;
score = 0; // Change this value.
// 3. Add explicit type annotations to these variables.
let favouriteLanguage = "TypeScript";
let hoursStudied = 1;
let lessonComplete = false;
// 4. Give each variable a different valid value.
favouriteLanguage = "TypeScript";
hoursStudied = 1;
lessonComplete = false;
// 5. Predict which lines below would cause a compiler error, and explain why.
//    Then uncomment them one at a time and run `tsc` to test your prediction.
//
// score = "ten";
// favouriteLanguage = 42;
// hoursStudied = 2;
// lessonComplete = true;
console.log("--- Lesson 1 exercises ---");
console.log(firstName, score);
console.log(favouriteLanguage, hoursStudied, lessonComplete);
/*
 * Bonus
 *
 * Create one string, one number, and one boolean without type annotations.
 * Hover over each variable in your editor and inspect the inferred type.
 */
//# sourceMappingURL=lesson-01-exercises.js.map